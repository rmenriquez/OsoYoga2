<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.osito.osoyoga">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActividadMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MudraActivity"
            android:label="MudraActivity">
        </activity>

        <activity android:name=".PosturaActivity"
            android:label="PosturaActivity">
        </activity>
        <activity android:name=".RespiracionesActivity"
            android:label="RespiracionesActivity">
        </activity>

        <activity android:name=".CountdownActivity"
            android:label="CountdownActivity">
        </activity>
    </application>

</manifest>